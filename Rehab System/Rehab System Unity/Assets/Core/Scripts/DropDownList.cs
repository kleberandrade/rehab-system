using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class DropDownList : MonoBehaviour
{
    /*
    [SerializeField]
    private GameObject m_Item;

    private GridLayoutGroup m_Grid;

    void Awake()
    {
        m_Grid = GetComponent<GridLayoutGroup>();
    }

    void Start()
    {
        RefreshList();
    }

    public void Add(string text, Sprite texture)
    {
        GameObject localItem = (GameObject)Instantiate(m_Item, Vector3.zero, Quaternion.identity);
        localItem.GetComponentInChildren<Text>().text = ;
        localItem.transform.SetParent(m_Grid.transform);
        localItem.transform.localScale = Vector3.one;
    }

    public void RefreshList()
    {
        XmlDocument xmlDoc = new XmlDocument();
        xmlDoc.LoadXml(m_XMLText.text);
        XmlNodeList games = xmlDoc.GetElementsByTagName("game");

        foreach (XmlNode game in games)
        {
            Debug.Log(game.ToString());
            
        }
    }
    */
}
